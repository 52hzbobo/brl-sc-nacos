# server
server:
  port: 12789

# Spring
spring:
  application:
    name: scbrl-cloud-zipkin
  profiles:
    active: dev
# 关闭自动配置启用所有请求得检测
management:
  metrics:
    web:
      server:
        auto-time-requests: false




